# RTK Database Configuration
# ==========================
# This is the master config for database credentials.
# Used by: setup.sh, rtk_init.sql
#
# IMPORTANT: After changing these values, run:
#   ./setup.sh sync
# to update these server config files:
#   - rtk/conf/char.conf
#   - rtk/conf/save.conf

# Database name
DB_NAME=RTK

# MySQL user for the game server
DB_USER=rtk

# Password for the game server user
# CHANGE THIS IN PRODUCTION!
DB_PASS=50LM8U8Poq5uX2AZJVKs

# MySQL host (use 127.0.0.1, not 'localhost' for WSL compatibility)
DB_HOST=127.0.0.1

# MySQL port
DB_PORT=3306

# MySQL root user (for initial setup only)
MYSQL_ROOT_USER=root
